export default function LoadingPage() {
  return (
    <p
      style={{
        textAlign: "center",
        fontSize: "var(--font-h1)",
        color: "var(--color-neutral-400)",
        fontWeight: 700,
        marginTop: 100,
      }}
    >
      Loading...
    </p>
  );
}
